<local:MyCard Title="{name}" Margin="0,0,0,15" CanSwap="False" IsSwapped="False">
<local:MyImage Height="50" HorizontalAlignment="Center" Source="{img}" />
<local:MyListItem Margin="0,0,0,0" Width="200"
                              Logo="pack://application:,,,/images/Blocks/CommandBlock.png"
                              Title="点我打开"
                              Info="{price}"
                              EventType="打开网页"
                              EventData="{url}"
                              Type="Clickable" />
</local:MyCard>
